Introduction

Kafka architecture

->Partitions

->Segments

->Consumer Group

->Cluster

->Leader-Follower Pattern

->Controller

->Kraft

->Log Compaction stratagies

->Why kafka is so fast

->Failure Scenarios

Complete cluster setup

Producer setup

Consumer setup

Exception handling








Event Driven Architecture(EDA)
-------------------------

services not called directly each other


Services
--------
OrderService

PaymentService

InventoryService

NotificationService


problems with synchronous chain
-------------------------------

1.Availability:

->All services must be available at the same time. Any service down, request will fail.

2.Latency Accumulation:

->Total Latency=sum of all service latencies.

3.Cascading Failure:

->One slow service can bring down other services and entire flow

4.Tight Coupling:

->In above example, Order service knows about Payment, Inventory and Notification service.
->Their endpoint, How to invoke, their response structure etc.

5.Scaling Issue:

->We can not scale one service independently based on load.


Services
--------

OrderService

EventRouter

PaymentService

InventoryService

NotificationService


Advantages of Event Driven Architecture:

1.Loose Coupling:

->No direct REST dependency.

2.Better Scalability:

->Services can scale independently.

3.Better Resilience:

->Temporary issue do not break the whole system.

4.Replay:

->We can re-process old event.

5.Improvement in latency.


Event Driven Architecture Components are:
-----------------------------------------

Producer->Event Router/Broker->Consumer1,Consumer2


How do events move through EDA
------------------------------


Push Model
----------

Producer->Broker->Consumer


Pull Model(Consumer process at own pace)
----------

Producer->Broker<-Consumer

Note: Consumer process at own pace that's why consumer arrow kept the reverse



EDA Models
----------

1.Pub/Sub:

->Events are published to active consumers and then forgotten.

->If new consumer joined say tomorrow, it wont get yesterday messages.

Used When:

->We care about just delivery.
->Not about storing history.

Example:
->RabbitMQ Exchange

2.Streaming:

->Events are appended in logs forever or retention based.

->If new consumer joined say tomorrow, it can read from latest offset or read from day1(if retention allows)

Used When:

->We want replay.
->We want multiple consumer

Example:
->Kafka


Challenges of Event Driven Architecture
---------------------------------------

1.Eventual Consistency:

->As events are processed in Async manner. Get call might return stale data, but sub-sequent calls might give up-to-date information.

2.Duplicate events:

->Most event router guarantee "at-least once delivery". do duplicates are normal.

3.Ordering problems:

->Events might reached in out-of-order.

4.Schema evolution:

->If schema breaks, all consumer will crash.

5.Debugging complexity:

->Distributed tracing across async hops are little difficult.

6.Poison messages:

->1 bad message can block the Event router.

7.Operational overhead:

->Must Monitor for:

a. Consumer lag

b. Throughput

c. Partition, Queue etc.


Use-cases of Event Driven Architecture, When to go for it:
----------------------------------------------------------

1. 1 event->N consumer: when one action must trigger multiple independent actions.
--------------------

EX:

OrderPlaced, then:

a. Inventory reserves
b. Payment charges
c. Email sent
d. Loyalty points added
e. Analytics updated

2. Long running business workflow:
----------------------------------

Ex:

order->payment->shipment->delivery

These flows take time and can fail midway. so flow is not in critical path, it can be broken into multi-step which is failure prone.


3. Eventual consistency is acceptable.

4. Real time analytics:

Data need to be continuously processed.






Producer



Topic->Partition

Topic->Partition->Offset

Topic->Partition->Segment->Offset

Topic->Partition->Segment->Index->Offset


Partition



storing stratagy in the partion
-------------------------------

1.Key-Based Partitioning(passing key)

2.Round Robin(loop)

3.Custom partitioning(India or USA)



Broker: Not store all the topics and all the partitions


ConsumerGroup rules
-------------------

1.Sinle Consumer

scenario:1

consumers=partitions

scenario:2

consumer<partitions

scenario:3

consumer>partitions


2.Multiple Consumers








ConsumerGroup->Topic->partition->commited offset(processed up to this offset)


note: not focus on the consumer in the ConsumerGroup


Offset commit stratagy
----------------------
1.Automatic
2.Manual



Partition divided into replicas

Partition(one leader and one follower)

controller decides who is leader and follower


controller

1.dual responsibility-------->normal broker+controller role

2.single responsibility-------->controller role


Kraft(modern)------->own

zookeeper(legacy)--------->external

Kraft consensus

Quorum

Active, Stand-By

ISR(In-Sync Replica)--------ACK

Partition(L, F, F)


completed
---------
1.producer

2.topic

3.partition-segment

4.Broker

5.Cluster

6.Controller

7.Kraft



Log Compaction strategies
-------------------------

Policy1: Delete(default)

Policy2: Compact


each one communicate with heart beat
------------------------------------

1.what happens when active controller fails

2.what happens when leader partition fails

3.what happens when follower partition fails

4.what happens when topic fails

5.what happens when consumer fails

6.what happens when broker fails




controller1.properties
----------------------

process.roles=controller

node.id=1

listeners=CONTROLLER://:9093

controller.listener.names=CONTROLLER

controller.quorum.voters=1@localhost:9093,2@localhost:9193


#PLAINTEXT (no encryption), in production, we should use SSL.
listener.security.protocol.map=CONTROLLER:PLAINTEXT

log.dirs=/tmp/controller1-logs


#Default partitions when creating a topic without specifying
num.partitions=3
default.replication.factor=2




controller2.properties
----------------------

process.roles=controller

node.id=2

listeners=CONTROLLER://:9093

controller.listener.names=CONTROLLER

controller.quorum.voters=1@localhost:9093,2@localhost:9193


#PLAINTEXT (no encryption), in production, we should use SSL.
listener.security.protocol.map=CONTROLLER:PLAINTEXT

log.dirs=/tmp/controller2-logs


#Default partitions when creating a topic without specifying
num.partitions=3
default.replication.factor=2



broker1.properties
------------------

process.roles=broker

node.id=3

#WHERE to fetch metadata from (which controllers to connect to)
controller.quorum.voters=1@localhost:9093,2@localhost:9193


#This is where our Spring Boot apps(producer/consumer) will connect
listeners=BROKER://:9092


#passed this address to controller to store in their cluster metadata
advertised.listeners=BROKER://localhost:9092


#internally when follower broker try to fetch from leader, what listener they need to use.
inter.broker.listener.name=BROKER

#when broker try to communicate with controller what listener they need to use.
controller.listener.names=CONTROLLER


listener.security.protocol.map=CONTROLLER:PLAINTEXT,BROKER:PLAINTEXT


log.dirs=/tmp/broker1-logs


#Keep messages for 7 days(168 hours)
log.retention.hours=168


#Max size of a single log segment file(1GB)
log.segment.bytes=1073741824



#__consumer_offsets topic RF
offsets.topic.replication.factor=2



broker2.properties
------------------

process.roles=broker

node.id=4

#WHERE to fetch metadata from (which controllers to connect to)
controller.quorum.voters=1@localhost:9093,2@localhost:9193


#This is where our Spring Boot apps(producer/consumer) will connect
listeners=BROKER://:9192


#passed this address to controller to store in their cluster metadata
advertised.listeners=BROKER://localhost:9192


#internally when follower broker try to fetch from leader, what listener they need to use.
inter.broker.listener.name=BROKER

#when broker try to communicate with controller what listener they need to use.
controller.listener.names=CONTROLLER


listener.security.protocol.map=CONTROLLER:PLAINTEXT,BROKER:PLAINTEXT


log.dirs=/tmp/broker2-logs


#Keep messages for 7 days(168 hours)
log.retention.hours=168


#Max size of a single log segment file(1GB)
log.segment.bytes=1073741824



#__consumer_offsets topic RF
offsets.topic.replication.factor=2








commands
--------


PS D:\kafka-setup\compose> docker run --rm apache/kafka:4.2.0 /opt/kafka/bin/kafka-storage.sh random-uuid

IVccCoWDRb2nIyFli3ewmg





docker run --rm `
-v ${PWD}/properties/controller1.properties:/opt/kafka-setup/properties/controller1.properties `
-v ${PWD}/data/controller1:/var/lib/kafka/data `
apache/kafka:4.2.0 `
/opt/kafka/bin/kafka-storage.sh format `
-t IVccCoWDRb2nIyFli3ewmg `
-c /opt/kafka-setup/properties/controller1.properties



docker run --rm `
-v ${PWD}/properties/controller2.properties:/opt/kafka-setup/properties/controller2.properties `
-v ${PWD}/data/controller2:/var/lib/kafka/data `
apache/kafka:4.2.0 `
/opt/kafka/bin/kafka-storage.sh format `
-t IVccCoWDRb2nIyFli3ewmg `
-c /opt/kafka-setup/properties/controller2.properties


docker run --rm `
-v ${PWD}/properties/broker1.properties:/opt/kafka-setup/properties/broker1.properties `
-v ${PWD}/data/broker1:/var/lib/kafka/data `
apache/kafka:4.2.0 `
/opt/kafka/bin/kafka-storage.sh format `
-t IVccCoWDRb2nIyFli3ewmg `
-c /opt/kafka-setup/properties/broker1.properties


docker run --rm `
-v ${PWD}/properties/broker2.properties:/opt/kafka-setup/properties/broker2.properties `
-v ${PWD}/data/broker2:/var/lib/kafka/data `
apache/kafka:4.2.0 `
/opt/kafka/bin/kafka-storage.sh format `
-t IVccCoWDRb2nIyFli3ewmg `
-c /opt/kafka-setup/properties/broker2.properties










D:\kafka-setup\compose> docker compose down    

PS D:\kafka-setup\compose> docker-compose build    
                                                      
PS D:\kafka-setup\compose> docker compose up -d  




D:\kafka-setup\compose>docker logs controller1












Kafka Producer Internal Working
-------------------------------


Kafka Consumer Internal Working
-------------------------------


Kafka Consumer Exception Handling With Dead Letter Queue
--------------------------------------------------------



Kafka Producer Transaction Handling(Coordinator, Epoch, PID, Zombie Fencing & 2PC)
-----------------------------------


Kafka Consumer Transaction Handling(EOS, LSO, Read Committed & Abort Handling)
-----------------------------------


Kafka Schema Registry With AVRO
-------------------------------



Outbox Pattern
--------------
1.Polling Strategy

2.Debezium Tool which implements CDC(Change Data Capture)

Note:

same like 

cdc(interface)

Debezium(class)
