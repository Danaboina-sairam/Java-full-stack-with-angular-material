
application.properties
----------------------

ReflectionTestUtils.setField(userService,"name","Sairam");









Test cases for void method
--------------------------

doNothing().when(digiLockerSVC).docUpload(productCode: "SBI Life EShield Insta", fileName: "01234_KFD.pdf", accountNumber: "123456", fileNameForDigi: "01234.pdf");








Test case for private method
----------------------------







Test case for public method
---------------------------








spy on method 
-------------









@Autowired / @InjectMocks / @Mock
---------------------------------


@Service
public class UserService {

    private final UserRepository userRepository;

    public UserService(UserRepository userRepository) {
        this.userRepository = userRepository;
    }

    public User getUser(Long id) {
        return userRepository.findById(id)
                .orElseThrow();
    }
}


@ExtendWith(MockitoExtension.class)
class UserServiceTest {

    @Mock
    private UserRepository userRepository;

    @InjectMocks
    private UserService userService;

    @Test
    void shouldReturnUser() {

        User user = new User(1L, "Sairam");

        when(userRepository.findById(1L))
                .thenReturn(Optional.of(user));

        User result = userService.getUser(1L);

        assertEquals("Sairam", result.getName());

        verify(userRepository).findById(1L);
    }
}




| Annotation     | Used By | Purpose                              |
| -------------- | ------- | ------------------------------------ |
| `@Autowired`   | Spring  | Injects actual Spring beans          |
| `@Mock`        | Mockito | Creates fake/mock dependency         |
| `@InjectMocks` | Mockito | Creates real class and injects mocks |



@Mock and @InjectMocks work only when Mockito is initialized:
-------------------------------------------------------------

@ExtendWith(MockitoExtension.class)












Put vs Get in HashMap Testcases
-------------------------------

Put: no need to mock the data because automatically data will set

Get: need to mock the data then only able to get








Setter vs Getter in DTO Testcases
---------------------------------

Setter: 

Getter:








Test cases for DateFormatter
----------------------------

Main Code side:
---------------

whatever formatter pattern define main code side

Test case side:
---------------

here date is input but output is same like main code side formatter






Some times NO need to write entire testcase but JUST called the method then also covered some lines
---------------------------------------------------------------------------------------------------







DTO
---

->some times automatically covers lines because of usage in the classes

->some times not cover automatically that time need to write setters and getters







Matcher and Non Matcher
-----------------------

String - matcher

List<Object> - matcher

HashMap- new HashMap<>()




Null response cases 
-------------------

test class
----------

@ExtendWith(MockitoExtension.class) : may be this is not used in the testing class

main code class
---------------

dependency injection issue in constructor injection










Unit Testing
------------


@ExtendWith(MockitoExtension.class)
class PolicyServiceTest {

    @Mock
    PolicyRepository repository;

    @InjectMocks
    PolicyService service;
}



Integration Testing/Slice Testing
---------------------------------


@SpringBootTest
class PolicyServiceTest {

    @MockBean
    PolicyRepository repository;

    @Autowired
    PolicyService service;
}
