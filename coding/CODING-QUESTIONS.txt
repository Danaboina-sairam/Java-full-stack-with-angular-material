Arrays
------

1.we have array of 0 and 1 then keee 0  at last and 1 at first

Normal Java:

public static void moveZeroes(int[] arr) {
    int count = 0;

    for (int num : arr) {
        if (num == 1) count++;
    }

    for (int i = 0; i < arr.length; i++) {
        if (i < count)
            arr[i] = 1;
        else
            arr[i] = 0;
    }
}

Java Streams:

int[] arr = {0,1,0,1,1,0,1};

int[] result = IntStream.concat(
        Arrays.stream(arr).filter(i -> i == 1),
        Arrays.stream(arr).filter(i -> i == 0)
).toArray();


2.find the the second max number in the array

3.we have 3 list then find out common number in 3 lists

4.Missing number in a consecutive array(Gauss sum/XOR approach)

5.Find the max element in a list

6.program to merge two sorted arrays into one

7.Write a program to print non repeating numbers from a given list

8.Reverse an array without using libraries and 3rd variable

9.find the two elements and their indexes in an array that sum up to a given target value

10.Find the first occurrence of number 1 from a sorted binary array and return index

11.find the smallest missing element in a given array and determine the time complexity


Strings
-------

1.long common subsequence

2.Write a program to reverse every word in a given String

3. Check if two strings are anagrams (sorting vs frequency array).

4.Find duplicate strings in a list (HashSet / frequency map)

5.Check whether a string is a palindrome (two-pointer approach)

6.Combination Sum II (backtracking + pruning)

7.Longest Common Prefix among strings (horizontal scanning/Trie optimization)

8.Longest Increasing Subsequence (DP + binary search optimization O(n log n))

9.Find the first non repetitive character in code(explain is its time complexity)

10.Write a program to remove duplicate characters from a string

11.Write a program to print the count of each character in a string

12.Write a program find the longest substring without duplicates

13.Find the anagrams in given array and group them into a list of lists


Object
------

Without grouping
-------------

1.Find the second highest salary from a given list of employees

2.Stream an employee list and filter emps having salary >10000 and sort them


With grouping
-------------


1.Use streams to fetch the employee records having average salary per department







Array Problems
--------------
1. Maximum Subarray Sum (Kadane’s Algorithm)
2. Rotate an Array by K Steps
3. Merge Intervals
4. Find the Duplicate Number
5. Maximum Product Subarray
6. Find the Missing and Repeating Number
7. Subarray with Given Sum
8. Longest Consecutive Sequence
9. Trapping Rain Water
10. Next Permutation

String Problems
---------------
11. Longest Palindromic Substring
12. Reverse Words in a String
13. Longest Common Prefix
14. Group Anagrams
15. Valid Parentheses Check
16. Implement atoi (String  Integer)
17. Longest Repeating Subsequence
18. KMP Pattern Searching Algorithm
19. Minimum Window Substring
20. String to Integer Conversion (Clarified task as distinct from atoi)

Linked List Problems
--------------------
21. Reverse a Linked List
22. Detect and Remove a Loop
23. Merge Two Sorted Linked Lists
24. Flatten a Multilevel Doubly Linked List
25. Intersection Point of Two Linked Lists
26. Remove Nth Node from End
27. Add Two Numbers Represented by Linked Lists
28. Clone a Linked List with Random Pointers
29. Sort a Linked List
30. Check if a Linked List Is Palindrome

Stack and Queue Problems
------------------------
31. Implement Stack Using Queues
32. Implement Queue Using Stacks
33. Next Greater Element
34. LRU Cache Implementation
35. Min Stack
36. Evaluate Reverse Polish Notation
37. Circular Queue Implementation
38. Sliding Window Maximum
39. Celebrity Problem
40. Largest Rectangle in Histogram

Binary Tree & BST Problems
--------------------------
41. Inorder, Preorder, Postorder Traversals
42. Level Order Traversal
43. Diameter of a Binary Tree
44. Lowest Common Ancestor (LCA)
45. Validate a Binary Search Tree
46. Serialize and Deserialize a Binary Tree
47. Zigzag Level Order Traversal
48. Kth Smallest Element in BST
49. Maximum Path Sum in a Binary Tree
50. Construct Binary Tree From Preorder + Inorder Traversals

Graph Problems
--------------
51. Breadth-First Search (BFS)
52. Depth-First Search (DFS)
53. Detect Cycle in Directed Graph
54. Detect Cycle in Undirected Graph
55. Dijkstra’s Shortest Path Algorithm

Dynamic Programming (DP) Problems
---------------------------------
56. 0/1 Knapsack
57. Longest Increasing Subsequence
58. Longest Common Subsequence
59. Edit Distance
60. Partition Equal Subset Sum

Searching & Sorting Problems
----------------------------
61. Binary Search
62. Merge Sort
63. Quick Sort
64. First and Last Position of Element in Sorted Array
65. Kth Smallest Element (General)
66. Search in Rotated Sorted Array

Backtracking Problems
---------------------
67. N-Queens
68. Sudoku Solver
69. Word Search
70. String Permutations
71. Generate All Subsets
72. Combination Sum
73. Rat in a Maze
74. Palindrome Partitioning
75. Knight’s Tour
76. M-Coloring Problem

Greedy Algorithm Problems
-------------------------
77. Activity Selection Problem
78. Fractional Knapsack
79. Huffman Coding / Encoding
80. Minimum Spanning Tree: Prim’s Algorithm
81. Minimum Spanning Tree: Kruskal’s Algorithm
82. Job Sequencing with Deadlines
83. Greedy Coloring of a Graph
84. Optimal File Merge Pattern
85. Dijkstra’s Algorithm (also greedy variant)
86. Gas Station Problem































Why does List have stream() but String doesn't?



Collection → stream()
Array → Arrays.stream()
String → chars(), codePoints(), lines() (Java 11+)


Arrays.stream()

Used to create a stream from an array.

String array
------------

String[] names = {"Java", "Spring", "SQL"};

Arrays.stream(names)
      .forEach(System.out::println);
















Group anagrams using Streams
----------------------------

import java.util.*;
import java.util.stream.Collectors;

public class GroupAnagrams {
    public static void main(String[] args) {

        List<String> words = Arrays.asList(
                "eat", "tea", "tan", "ate", "nat", "bat"
        );

        Map<String, List<String>> result = words.stream()
                .collect(Collectors.groupingBy(word -> {
                    char[] chars = word.toCharArray();
                    Arrays.sort(chars);
                    return new String(chars);
                }));

        System.out.println(result);
    }
}



Find the second longest word
-----------------------------

String str = "Java SpringBoot Microservices Hibernate Kafka";

String secondLongest = Arrays.stream(str.split("\\s+"))
        .distinct()
        .sorted(Comparator.comparingInt(String::length).reversed())
        .skip(1)
        .findFirst()
        .orElse(null);

System.out.println(secondLongest);






Find the first duplicate word
-----------------------------

import java.util.*;
import java.util.stream.*;

public class FirstDuplicateWord {
    public static void main(String[] args) {

        String sentence = "Java is easy Java and Streams are easy";

        Set<String> seen = new HashSet<>();

        Optional<String> firstDuplicate = Arrays.stream(sentence.split("\\s+"))
                .filter(word -> !seen.add(word))
                .findFirst();

        firstDuplicate.ifPresentOrElse(
                System.out::println,
                () -> System.out.println("No duplicate word found")
        );
    }
}

Remove duplicate words while preserving order
---------------------------------------------

import java.util.Arrays;
import java.util.stream.Collectors;

public class RemoveDuplicateWords {
    public static void main(String[] args) {
        String str = "java is easy java is powerful easy language";

        String result = Arrays.stream(str.split("\\s+"))
                              .distinct()
                              .collect(Collectors.joining(" "));

        System.out.println(result);
    }
}


Count occurrences of each word
------------------------------

import java.util.Arrays;
import java.util.Map;
import java.util.function.Function;
import java.util.stream.Collectors;

public class WordCount {
    public static void main(String[] args) {

        String str = "java is easy java is powerful java";

        Map<String, Long> wordCount = Arrays.stream(str.split("\\s+"))
                .collect(Collectors.groupingBy(
                        Function.identity(),
                        Collectors.counting()));

        System.out.println(wordCount);
    }
}


Find the most frequent character
--------------------------------

import java.util.*;
import java.util.function.Function;
import java.util.stream.Collectors;

public class MostFrequentCharacter {
    public static void main(String[] args) {
        String str = "programming";

        Map.Entry<Character, Long> result = str.chars()
                .mapToObj(c -> (char) c)
                .collect(Collectors.groupingBy(
                        Function.identity(),
                        LinkedHashMap::new,
                        Collectors.counting()))
                .entrySet()
                .stream()
                .max(Map.Entry.comparingByValue())
                .orElse(null);

        if (result != null) {
            System.out.println(result.getKey() + " -> " + result.getValue());
        }
    }
}

Find the least frequent character
---------------------------------

import java.util.*;
import java.util.function.Function;
import java.util.stream.Collectors;

public class LeastFrequentCharacter {
    public static void main(String[] args) {
        String str = "programming";

        // Count frequency of each character
        Map<Character, Long> frequencyMap = str.chars()
                .mapToObj(c -> (char) c)
                .collect(Collectors.groupingBy(
                        Function.identity(),
                        LinkedHashMap::new,
                        Collectors.counting()));

        // Find minimum frequency
        long minFrequency = frequencyMap.values()
                .stream()
                .min(Long::compare)
                .orElse(0L);

        // Print all least frequent characters
        frequencyMap.entrySet()
                .stream()
                .filter(entry -> entry.getValue() == minFrequency)
                .forEach(entry ->
                        System.out.println(entry.getKey() + " = " + entry.getValue()));
    }
}



Check if two strings are anagrams
---------------------------------


import java.util.Arrays;
import java.util.stream.Collectors;

public class AnagramCheck {
    public static void main(String[] args) {
        String str1 = "listen";
        String str2 = "silent";

        String sorted1 = Arrays.stream(str1.toLowerCase().split(""))
                               .sorted()
                               .collect(Collectors.joining());

        String sorted2 = Arrays.stream(str2.toLowerCase().split(""))
                               .sorted()
                               .collect(Collectors.joining());

        System.out.println(sorted1.equals(sorted2)
                ? "Anagrams"
                : "Not Anagrams");
    }
}




Find all palindromic words in a sentence
----------------------------------------

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class PalindromicWords {
    public static void main(String[] args) {

        String sentence = "madam speaks level malayalam and radar in civic center";

        List<String> palindromes = Arrays.stream(sentence.split("\\s+"))
                .filter(word -> word.length() > 1)
                .filter(word -> word.equalsIgnoreCase(
                        new StringBuilder(word).reverse().toString()))
                .collect(Collectors.toList());

        System.out.println(palindromes);
    }
}


Sort words by frequency
-----------------------

import java.util.*;
import java.util.function.Function;
import java.util.stream.Collectors;

public class Main {
    public static void main(String[] args) {

        String str = "java python java spring java python streams";

        Map<String, Long> result = Arrays.stream(str.split("\\s+"))
                .collect(Collectors.groupingBy(
                        Function.identity(),
                        Collectors.counting()))
                .entrySet()
                .stream()
                .sorted(Map.Entry.<String, Long>comparingByValue(Comparator.reverseOrder()))
                .collect(Collectors.toMap(
                        Map.Entry::getKey,
                        Map.Entry::getValue,
                        (a, b) -> a,
                        LinkedHashMap::new));

        result.forEach((k, v) -> System.out.println(k + " = " + v));
    }
}


Reverse each word in a sentence
-------------------------------

import java.util.Arrays;
import java.util.stream.Collectors;

public class ReverseEachWord {
    public static void main(String[] args) {
        String sentence = "Reverse each word in a sentence.";

        String result = Arrays.stream(sentence.split("\\s+"))
                .map(word -> new StringBuilder(word).reverse().toString())
                .collect(Collectors.joining(" "));

        System.out.println("Original : " + sentence);
        System.out.println("Reversed : " + result);
    }
}


Find the longest common prefix using Streams
--------------------------------------------

import java.util.Arrays;
import java.util.Optional;

public class LongestCommonPrefix {

    public static void main(String[] args) {

        String[] arr = {"flower", "flow", "flight"};

        Optional<String> result = Arrays.stream(arr)
                .reduce((s1, s2) -> {
                    int len = java.util.stream.IntStream
                            .range(0, Math.min(s1.length(), s2.length()))
                            .takeWhile(i -> s1.charAt(i) == s2.charAt(i))
                            .count();

                    return s1.substring(0, (int) len);
                });

        System.out.println(result.orElse(""));
    }
}



Partition words into even/odd lengths
-------------------------------------

import java.util.*;
import java.util.stream.*;

public class PartitionWords {
    public static void main(String[] args) {
        String sentence = "Java Streams make coding easier and cleaner";

        Map<Boolean, List<String>> result = Arrays.stream(sentence.split("\\s+"))
                .collect(Collectors.partitioningBy(word -> word.length() % 2 == 0));

        System.out.println("Even Length Words: " + result.get(true));
        System.out.println("Odd Length Words : " + result.get(false));
    }
}


Group words by first character
------------------------------

import java.util.*;
import java.util.stream.Collectors;

public class GroupByFirstCharacter {
    public static void main(String[] args) {

        String sentence = "apple ant ball bat cat car dog";

        Map<Character, List<String>> result = Arrays.stream(sentence.split("\\s+"))
                .collect(Collectors.groupingBy(word -> word.charAt(0)));

        System.out.println(result);
    }
}


Flatten a List<String> into a stream of characters using flatMap
----------------------------------------------------------------

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class FlatMapCharacters {
    public static void main(String[] args) {

        List<String> words = Arrays.asList("Java", "Stream", "API");

        List<Character> characters = words.stream()
                .flatMap(str -> str.chars()
                        .mapToObj(ch -> (char) ch))
                .collect(Collectors.toList());

        System.out.println(characters);
    }
}




Print repeated characters only (distinct)
-----------------------------------------


import java.util.function.Function;
import java.util.stream.Collectors;

String input = "programming";

input.chars()
     .mapToObj(c -> (char) c)
     .collect(Collectors.groupingBy(
             Function.identity(),
             Collectors.counting()))
     .entrySet()
     .stream()
     .filter(entry -> entry.getValue() > 1)
     .map(entry -> entry.getKey())
     .forEach(System.out::println);

